# =============================================================================
# Kwami AI API - Environment Variables
# =============================================================================
#
# Copy this file to .env and fill in your credentials:
#   cp .env.sample .env
#
# =============================================================================

# =============================================================================
# App Configuration
# =============================================================================
APP_ENV=development
DEBUG=true

# API Server
API_HOST=0.0.0.0
API_PORT=8080

# CORS (comma-separated origins, use * for development)
CORS_ORIGINS=*

# =============================================================================
# LiveKit Configuration (required for token generation)
# =============================================================================
# Get these from https://cloud.livekit.io/projects/YOUR_PROJECT/settings/keys

LIVEKIT_URL=wss://your-project.livekit.cloud
LIVEKIT_API_KEY=your-api-key
LIVEKIT_API_SECRET=your-api-secret

# =============================================================================
# Zep Cloud (required for memory endpoints)
# =============================================================================
# Get your API key:
#   1. Create account at https://app.getzep.com/
#   2. Go to Project Settings > API Keys
#   3. Create a new API key

ZEP_API_KEY=z_your-zep-api-key

# =============================================================================
# Supabase Auth
# =============================================================================
# SUPABASE_URL: Required for asymmetric key verification (RS256, ES256)
#   Get from Supabase Dashboard: Project Settings > API > Project URL
#
# SUPABASE_JWT_SECRET: Required for symmetric key verification (HS256)
#   Get from Supabase Dashboard: Project Settings > API > JWT Settings > JWT Secret
#   Note: Supabase recommends using asymmetric keys instead of the JWT secret.

SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_JWT_SECRET=your-jwt-secret
